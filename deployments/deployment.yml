apiVersion: apps/v1
kind: Deployment
metadata:
  name: phatfpt-capstone-final-project
  labels:
    app: phatfpt-capstone-final-project
  namespace: default
spec:
  replicas: 2
  progressDeadlineSeconds: 3600
  selector:
    matchLabels:
      app: uda-cap
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: phatfpt-capstone-final-project
    spec:
      containers:
        - image: phat99/phatfpt-capstone-final-project
          imagePullPolicy: Always
          name: phatfpt-capstone-final-project
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: phatfpt-capstone-final-project
  labels:
    app: phatfpt-capstone-final-project
spec:
  type: LoadBalancer
  selector:
    app: phatfpt-capstone-final-project
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
