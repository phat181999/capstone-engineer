AWSTemplateFormatVersion: 2010-09-09
Parameters:
  ImageTag:
    Type: String
    Description: tag of the docker image to deploy in this stack
    Default: latest

Resources:
  TaskDefMyApp:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: MyApp
      ContainerDefinitions:
        - Image: !Sub "$AWS_ECR_URL/eks_orb_capstone_project:a846f2152be32e72b75e94ac8110fb7adc8256a2"
