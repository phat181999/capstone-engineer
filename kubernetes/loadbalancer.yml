apiVersion: v1
kind: Service
metadata:
  name: phatht8-casptone
  labels:
    app: phatht8-casptone
spec:
  type: LoadBalancer
  selector:
    app: phatht8-casptone
  ports:
    - port: 80
      targetPort: 80
